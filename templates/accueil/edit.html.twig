{% extends 'base.html.twig' %}

{% block title %}Edit Accueil{% endblock %}

{% block body %}
    <h1>Edit Accueil</h1>

    {{ include('accueil/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('accueil_index') }}">back to list</a>

    {{ include('accueil/_delete_form.html.twig') }}
{% endblock %}